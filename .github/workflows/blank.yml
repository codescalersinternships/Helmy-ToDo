name: CI Workflow

on:
  push:
    branches:  
      - "main"

jobs:

  build:

    runs-on: ubuntu-18.04

    steps:
    - uses: actions/checkout@v3
    -
        name: Build and push backend
        run: cd backend
    -   
        name: check dir
        run: pwd
